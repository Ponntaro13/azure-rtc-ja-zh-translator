{
  "name": "azure-rtc-ja-zh-translator",
  "private": true,
  "scripts": {
    "dev": "concurrently -n API,WEB -c blue,green \"npm run dev --prefix api\" \"npm run dev --prefix web\"",
    "build": "npm run build --prefix api && npm run build --prefix web"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
